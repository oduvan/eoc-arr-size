
export function arrSize(els: number[]): number {
    // your code here
    return undefined;
}

import * as assert from 'assert';

console.log('Example:');
console.log(arrSize([1, 2, 3]));

// These "asserts" are used for self-checking and not for an auto-testing
assert.equal(arrSize([1, 2, 3]), 3);
assert.equal(arrSize([]), 0);

console.log("Coding complete? Click 'Check' to earn cool rewards!");
    